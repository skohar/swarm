---
- name: Install init config
  become: yes
  template:
    src: codecheck.conf.j2
    dest: /etc/init/codecheck.conf
- name: Start test run server
  become: yes
  service:
    name: codecheck
    state: started

