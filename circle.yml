machine:
  services:
    - docker
dependencies:
  pre:
    - sudo pip install ansible
test:
  override:
    - ./circle-test.sh
deployment:
  production:
    branch: deployment/production
    commands: 
      - date
  staging:
    branch: master
    commands:
     - packer build packer.json
