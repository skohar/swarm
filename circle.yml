machine:
  python:
    version: 2.7.10
  environment:
    PATH: "${HOME}/bin:${PATH}"
dependencies:
  cache_directories:
    - "~/bin"
  pre:
    - mkdir -p ~/bin
    - ./dependencies.sh
test:
  override:
    - ./packer-validate.sh
deployment:
  production:
    branch: deployment/production
    commands: 
      - date
  staging:
    branch: packer
    commands:
     - packer build packer.json
